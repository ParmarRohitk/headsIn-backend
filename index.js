"use strict";

/**
 * Render sometimes defaults to running 'node index.js' if not explicitly configured.
 * This file acts as a proxy to the actual entry point in src/server.js.
 */
require("./src/server.js");
